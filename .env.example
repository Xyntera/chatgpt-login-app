# OPTIONAL on Vercel — VERCEL_URL is auto-detected if this is unset.
# Set explicitly for local dev or a custom domain. No trailing slash.
# Dev:  http://localhost:3000
# Prod: https://yourdomain.com
NEXT_PUBLIC_APP_ORIGIN=http://localhost:3000
